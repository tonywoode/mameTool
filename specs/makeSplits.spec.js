'use strict'

const {applySplit} = require('../src/makeSplits.js')

const tick       =  {name: `series`, value: 1}
const outputDir  = `./outputs`
const winIconDir = `F:\MAME\EXTRAs\Icons`
const mameJson   = require('../outputs/mame.json')

//console.log(mameJson)
//applySplit( tick, outputDir, emu, winIconDir, mameJson)
//console.log(done)
